library(shiny)
shinyUI(fluidPage(
  
  titlePanel(title = "DP Algorithm - Mining in Mozambique"),
  sidebarLayout(
    sidebarPanel("sidebar example"),
    mainPanel("mainpanel text and output is displayed here")
    
    )
  )

  )

